services:
  - type: web
    name: vaultguard-backend
    runtime: node
    buildCommand: npm run build
    startCommand: npm run start:production
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000